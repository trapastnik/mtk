services:
  web:
    image: nginx:alpine
    container_name: mtk-web
    ports:
      - "8097:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - .:/usr/share/nginx/html:ro
    restart: always
